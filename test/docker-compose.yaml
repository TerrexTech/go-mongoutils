version: '3'

services:
  mongodb:
    image: mongo
    container_name: mongoutils_mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
    ports:
      - "27017:27017"
